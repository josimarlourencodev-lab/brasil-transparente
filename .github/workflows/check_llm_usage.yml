name: Verificar persistência do orçamento (Supabase)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: supabase-migrate
  cancel-in-progress: false

jobs:
  verifica:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    env:
      SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
      NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Instalar dependência HTTP
        run: pip install -r scripts/requirements.lock.leve.txt

      - name: Testar leitura e escrita via RPC anon (llm_usage)
        env:
          LLM_BUDGET_FILE: /tmp/budget.json
        run: |
          python - <<'PY'
          import sys
          sys.path.insert(0, "scripts")
          from synthesizer import _llm_usage_supabase, _record_llm_usage_supabase

          antes = _llm_usage_supabase()
          print("RPC soma tokens 24h antes  :", antes)

          # Sonda idempotente: 0 tokens não altera o orçamento, só prova a escrita.
          ok = _record_llm_usage_supabase(0)
          print("RPC registro              :", ok)

          depois = _llm_usage_supabase()
          print("RPC soma tokens 24h depois:", depois)

          if antes is None or not ok or depois is None:
              print("FALHOU: persistência no Supabase não confirmada.")
              sys.exit(1)
          print("OK: RPC llm_usage_sum_since/record_llm_usage acessíveis via anon.")
          PY