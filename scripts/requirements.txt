## Arquivo de entrada (pins diretos). O lock com hashes é requirements.lock.txt
##
## pip install -r requirements.txt   → instala com verificação rigorosa de hashes.
--require-hashes
-r requirements.lock.txt